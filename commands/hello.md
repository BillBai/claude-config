---
description: "Example command - greets the user"
---

# Hello Command

This is an example command to demonstrate how claude-config plugin works.

Say hello to the user and explain that this is a custom command from the claude-config plugin.

You can create your own commands by adding markdown files to the `commands/` directory.
